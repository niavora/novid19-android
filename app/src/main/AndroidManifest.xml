<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qr_niavo">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- INTERNET -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_novid"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_novid_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QR_NIAVO"
        android:usesCleartextTraffic="true">
        <activity android:name=".ListActivity"></activity>
        <activity android:name=".ListeLieu" />
        <activity android:name=".Resultat" />
        <activity android:name=".AccueilActivity" />
        <activity android:name=".ListeTest" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
    </application>

</manifest>